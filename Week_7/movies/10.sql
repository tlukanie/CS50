SELECT DISTINCT people.name FROM people JOIN directors ON directors.person_id = people.id, movies ON directors.movie_id = movies.id, ratings ON ratings.movie_id = movies.id WHERE ratings.rating>= 9.0;